name: main

on: [push]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-18.04

    steps:
    - uses: actions/checkout@v1

    - name: lint
      uses: ansible/ansible-lint-action@v4.1.0
      with:
        target: debian.yml
